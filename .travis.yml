branches:
  only:
    - master
    - dev

notifications:

language: cpp

compiler:
  - gcc
  - clang

addons:
  apt:
    packages:
    - libsdl2-dev
    - libsdl2-image-dev
    - libsdl2-ttf-dev
    - libluajit-5.1-dev
    - libphysfs-dev
    - libopenal-dev
    - libmodplug-dev
    - libvorbis-dev
    - qtbase5-dev
    - qttools5-dev
    - qttools5-dev-tools

script: mkdir build && cd build && cmake -DCMAKE_BUILD_TYPE=Debug .. && cmake -DRAKNET_LIBRARY=../libraries/linux/libRakNetLibStatic.a && make && ctest -V

